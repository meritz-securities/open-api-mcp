# 보안 정책

메리츠증권 Open API 저장소의 취약점 신고 및 App Key 관리 정책입니다.

## 취약점 신고

인증 우회, 자격증명 노출, 권한 상승과 관련된 취약점을 발견한 경우 공개 이슈를 등록하지 말고 개발자 포털의 문의 채널로 신고하십시오.

- 개발자 포털: https://openapi.imeritz.com

신고 시 재현 절차와 예상 영향 범위를 함께 제공해 주시기 바랍니다.

## 앱키(App Key / Secret) 관리

- App Key와 App Secret은 인증 자격증명으로 취급하십시오. 코드, 저장소, 로그, 공유 채널에 기록하지 마십시오.
- 수동 실행 시에는 환경변수(`MERITZ_APP_KEY`, `MERITZ_APP_SECRET`)를 사용하십시오.
- 노출이 의심되는 경우 개발자 포털에서 즉시 재발급하십시오.

## 주문 요청 유의

호출 대상은 **운영 서버**(`openapi.imeritz.com:9443`)입니다. 모든 요청이 실계좌로
나간다고 전제하십시오.

주문·환전은 미리보기에서 받은 `confirm_token` 이 있어야 전송되며, 이 절차를 끄는
설정은 없습니다. 다만 **에이전트가 스스로 확인 후 즉시 전송하는 것은 막지 못합니다.**
무인 환경에서는 `MERITZ_READ_ONLY=1` 로 막으십시오.

## 이 서버에는 자체 인증이 없습니다

Streamable HTTP 로 띄울 때 로컬 포트를 인터넷에 직접 공개하지 마십시오.
**연결할 수 있는 사람은 곧 주문할 수 있는 사람입니다.**

기본값은 `MERITZ_READ_ONLY=1`(조회 전용)입니다. 주문·환전까지 쓰시려면
`MERITZ_READ_ONLY=0` 으로 명시적으로 여셔야 합니다. 공용 인터넷에 노출해야 한다면
사용자별 인증·TLS·접근제어가 적용된 프록시 뒤에 배치하십시오.
