# 기여 안내

메리츠증권 Open API MCP 서버에 관심 가져 주셔서 감사합니다.
이 문서는 이슈를 올리시거나 코드를 보내실 때 참고하실 내용을 담았습니다.

## 먼저 확인해 주십시오

**계좌·주문·거래에 관한 문의는 이곳에서 처리해 드릴 수 없습니다.**

- 주문이 나가지 않았다, 잔고나 평가금액이 다르게 보인다, 체결 내역을 확인하고 싶다
- 앱키 발급, 계좌 연결, 이용 신청, 권한 부족
- API 명세·업무 규칙에 대한 문의

이런 내용은 **개발자 포털의 문의 채널**을 이용해 주십시오. 이 저장소는 MCP 서버
소프트웨어만 다루며, 저장소 관리자에게는 계좌를 조회하거나 주문 상태를 확인할
권한이 없습니다.

이곳에서 다루는 것은 **이 서버 소프트웨어**에 관한 문제입니다.

## 자격증명과 계좌 정보

이슈·PR·커밋에 다음을 절대 넣지 말아 주십시오.

- 앱키(App Key)·시크릿(App Secret)·접근토큰
- 계좌번호
- 도구 응답에 담긴 잔고·보유종목·주문번호 등 실제 값

**MCP 설정 파일에는 앱키가 그대로 적혀 있습니다.** 설정을 옮겨 붙이실 때 특히
주의해 주십시오. 이슈는 누구나 볼 수 있고, 지우셔도 수정 이력에 남습니다.
이미 노출하셨다면 개발자 포털에서 **즉시 재발급**해 주십시오.

## 이슈

이슈 양식을 골라 주십시오. 빈 이슈는 열어 두지 않았습니다.

| 양식 | 언제 |
|---|---|
| 서버가 동작하지 않습니다 | 설치·연결·도구 호출에서 생긴 문제 |
| 도구 설명·문서가 실제와 다릅니다 | `describe` 설명이나 README 가 실제와 어긋날 때 |
| 이런 기능이 있었으면 합니다 | 새 도구 제안, 개선 제안 |

**어느 클라이언트인지(Claude Desktop·Claude Code·Cursor·VS Code·Codex CLI)**,
어느 서버였는지(운영·개발), 호출하신 도구와 `api_type`, 서버 버전, 파이썬 버전을
적어 주시면 확인이 훨씬 빠릅니다.

## 개발 환경

Python 3.11 이상과 [uv](https://docs.astral.sh/uv/) 가 필요합니다.

```bash
git clone https://github.com/meritz-securities/open-api-mcp.git
cd open-api-mcp
uv sync
```

## 테스트

```bash
uv run pytest tests/ -q
```

앱키나 네트워크 없이 돕니다. `tests/test_contract.py` 는 도구 정의와 카탈로그가
서로 맞는지, 응답 판정과 확인 절차가 약속대로 동작하는지를 봅니다.

**테스트를 위해 실제 API 를 호출하지 말아 주십시오.** 앱키에 연결된 계좌는
실계좌입니다.

## 코드를 보내실 때

이 서버는 실제 계좌에 주문을 낼 수 있습니다. 아래 약속은 안전 장치이므로 지켜 주십시오.

- **주문·환전의 두 단계 확인 절차를 우회하거나 약화시키지 말아 주십시오.**
  첫 호출은 전송하지 않고 `confirm_token` 만 돌려주며, 토큰은 한 번만 쓰이고
  180초 뒤 만료됩니다. 본문이나 대상 서버가 바뀌면 무효가 됩니다
- 상태를 바꾸는 호출인지는 **경로와 HTTP 메서드를 둘 다** 보아 판정합니다.
  한쪽만 보도록 바꾸지 말아 주십시오
- `MERITZ_READ_ONLY` 의 기본값은 `1`(조회 전용)입니다. 바꾸지 말아 주십시오
- 응답 판정 순서를 지켜 주십시오. 주문은 `warn_cls_code` 를 `rsp_cd` 보다
  **먼저** 봅니다. 뒤에 두면 걸리지 않습니다
- HTTP 200 을 성공으로 넘기지 말아 주십시오. 초당 호출 한도를 넘기면 500 과 함께
  `EGW00200` 이 옵니다
- 코드 스타일은 `pyproject.toml` 의 ruff 설정을 따릅니다 (줄 길이 120)

도구를 더하거나 고치셨다면 `describe` 로 나가는 설명과 README 도 함께 고쳐 주십시오.

## 브랜치와 PR

1. 저장소를 포크하고 브랜치를 만들어 주십시오
2. 변경 후 `uv run pytest tests/ -q` 를 돌려 주십시오
3. 가능하시면 실제 클라이언트에 붙여 한 번 확인해 주십시오
4. PR 양식을 채워 보내 주십시오

## 보안 취약점

인증 우회·자격증명 노출·권한 상승, 확인 절차 우회와 관련된 내용은 **공개 이슈나
PR 로 올리지 마시고**, [SECURITY.md](SECURITY.md) 에 적힌 절차를 따라 주십시오.

## 라이선스

기여하신 내용은 이 저장소와 같은 [MIT 라이선스](LICENSE)로 배포됩니다.
